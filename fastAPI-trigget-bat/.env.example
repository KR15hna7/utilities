# Azure Storage Configuration Template
# Copy this file to .env and replace the placeholder values with your actual Azure Storage account details

# Azure Storage Connection String
# Get this from Azure Portal > Storage Account > Access keys
AZURE_STORAGE_CONNECTION_STRING=example

# Azure Storage Container Name
# The name of the blob container where JSON files will be uploaded
AZURE_STORAGE_CONTAINER=example
# Optional: Set a custom port for the FastAPI server (default is 8000)
# SERVER_PORT=8000

# Optional: Set a custom host for the FastAPI server (default is 0.0.0.0)
# SERVER_HOST=0.0.0.0